export interface ProductType {
  productId: number;
  productName: string;
  productImage: string;
  productDescription: string;
  productStock: number;
  createdDate: string;
  updatedDate: string;
}
